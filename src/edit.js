// TODO - Falta habilitar los campos de la tarea y traer el array para actualizarlo
export function editTask(element) {
    if (element.name === "Editar") {
      //element.parentElement.parentElement.remove();
      console.log("editandooo");
    }
}
